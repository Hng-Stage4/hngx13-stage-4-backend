# ============================================
# docker-compose.yml (Root level)
# Place this at: hngx13-stage-4-backend/docker-compose.yml
# ============================================

services:
  # API Gateway
  api-gateway:
    build:
      context: ./services/api_gateway
      dockerfile: Dockerfile
    container_name: api-gateway
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
      - RABBITMQ_HOST=rabbitmq
      - REDIS_HOST=redis
      - USER_SERVICE_URL=http://user-service:8001
    depends_on:
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./services/api_gateway:/app
    networks:
      - notification-network
    restart: unless-stopped

  # RabbitMQ
  rabbitmq:
    image: rabbitmq:3.12-management-alpine
    container_name: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - notification-network
    restart: unless-stopped

  # Redis
  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - notification-network
    restart: unless-stopped

  # PostgreSQL (for User Service and Template Service)
  postgres:
    image: postgres:15-alpine
    container_name: postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=secret
      - POSTGRES_DB=notifications
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - notification-network
    restart: unless-stopped

volumes:
  rabbitmq_data:
  redis_data:
  postgres_data:

networks:
  notification-network:
    driver: bridge


# ============================================
# Alternative: docker-compose.yml in services/api_gateway/
# If you want to run it from the api_gateway directory
# ============================================

# services:
#   # API Gateway
#   api-gateway:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     container_name: api-gateway
#     ports:
#       - "3000:3000"
#     environment:
#       - DEBUG=True
#       - RABBITMQ_HOST=rabbitmq
#       - REDIS_HOST=redis
#       - USER_SERVICE_URL=http://user-service:8001
#     depends_on:
#       rabbitmq:
#         condition: service_healthy
#       redis:
#         condition: service_healthy
#     volumes:
#       - .:/app
#     networks:
#       - notification-network
#     restart: unless-stopped
# 
#   # Same RabbitMQ, Redis, Postgres as above...