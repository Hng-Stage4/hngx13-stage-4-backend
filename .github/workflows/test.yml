  # # ==========================================
  # # LINT AND TEST
  # # ==========================================
  # test:
  #   name: Test Services
  #   runs-on: ubuntu-latest
  #   needs: detect-changes
  #   if: |
  #     needs.detect-changes.outputs.api_gateway == 'true' ||
  #     needs.detect-changes.outputs.user_service == 'true' ||
  #     needs.detect-changes.outputs.email_service == 'true' ||
  #     needs.detect-changes.outputs.push_service == 'true' ||
  #     needs.detect-changes.outputs.template_service == 'true'
    
  #   strategy:
  #     matrix:
  #       service:
  #         - name: api_gateway
  #           path: services/api_gateway
  #           port: 3000
  #         - name: user_service
  #           path: services/user_service
  #           port: 8001
  #         - name: email_service
  #           path: services/email_service
  #           port: 8002
  #         - name: push_service
  #           path: services/push_service
  #           port: 8002
  #         - name: template_service
  #           path: services/template_service
  #           port: 8003
    
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
      
  #     - name: Check if service changed
  #       id: check
  #       run: |
  #         if [ "${{ needs.detect-changes.outputs[matrix.service.name] }}" == "true" ]; then
  #           echo "changed=true" >> $GITHUB_OUTPUT
  #         else
  #           echo "changed=false" >> $GITHUB_OUTPUT
  #         fi
      
  #     - name: Set up Python
  #       if: steps.check.outputs.changed == 'true'
  #       uses: actions/setup-python@v4
  #       with:
  #         python-version: '3.11'
      
  #     - name: Cache dependencies
  #       if: steps.check.outputs.changed == 'true'
  #       uses: actions/cache@v3
  #       with:
  #         path: ~/.cache/pip
  #         key: ${{ runner.os }}-pip-${{ hashFiles(format('{0}/requirements.txt', matrix.service.path)) }}
  #         restore-keys: |
  #           ${{ runner.os }}-pip-
      
  #     - name: Install dependencies
  #       if: steps.check.outputs.changed == 'true'
  #       working-directory: ${{ matrix.service.path }}
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install -r requirements.txt
  #         pip install pytest pytest-asyncio pytest-cov flake8 black
      
  #     - name: Run Black formatter check
  #       if: steps.check.outputs.changed == 'true'
  #       working-directory: ${{ matrix.service.path }}
  #       run: black --check app/ || true
      
  #     - name: Run Flake8 linter
  #       if: steps.check.outputs.changed == 'true'
  #       working-directory: ${{ matrix.service.path }}
  #       run: flake8 app/ --max-line-length=100 --ignore=E501,W503 || true
      
  #     - name: Run tests
  #       if: steps.check.outputs.changed == 'true'
  #       working-directory: ${{ matrix.service.path }}
  #       run: |
  #         pytest tests/ -v --cov=app --cov-report=xml --cov-report=term || true
      
  #     - name: Upload coverage
  #       if: steps.check.outputs.changed == 'true'
  #       uses: codecov/codecov-action@v3
  #       with:
  #         file: ${{ matrix.service.path }}/coverage.xml
  #         flags: ${{ matrix.service.name }}
  #         name: ${{ matrix.service.name }}-coverage